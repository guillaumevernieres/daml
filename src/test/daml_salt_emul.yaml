ffnn:
  inputSize: 20
  outputSize: 20
  hiddenSize: 1
  kernelSize: 5
  stride: 1'
#  load model: "model.pt"

prediction:
#  output filename: "salt.ffnn.nc"
  mom6 restart: "/home/gvernier/data/woa/woa_ts.nc"

training:
  epochs: 30000
  batch size: 300
  mom6 restart: "/home/gvernier/data/woa/woa_ts.nc"
  model output: "model.pt"
